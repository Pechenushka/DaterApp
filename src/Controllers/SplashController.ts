import {BaseController} from './BaseController';

class SplashController extends BaseController {
  //CUSTOM CODE

  constructor() {
    super({id: 'SplashController', onBackPress: () => void 0});
  }
}

export {SplashController};
